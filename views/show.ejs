<!DOCTYPE html>
<html lang="en">
  <%- include("./partials/head.ejs") %>
  <body>
    <h1><%= pokemon.name %></h1>
    <div class="poke-data">
      <div class="poke-image">
        <img src="<%= pokemon.img %>" alt="<%= pokemon.name %>" />
      </div>
      
    </div>
  </body>
</html>
