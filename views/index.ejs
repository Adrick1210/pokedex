<!DOCTYPE html>
<html lang="en">
  <head>
    <%- include("./partials/head.ejs") %>
  </head>
  <body>
    <h1>Choose that Pokémon</h1>

    <div class="poke-container">
      <ul class="poke-list">
        <%for (let i = 0; i < pokemon.length; i++) { %>
        <a href="/pokedex/<%= i %>"
          ><img src="<%= pokemon[i].img %>" alt="<%= pokemon[i].name %>"
        /></a>
        <% } %>
      </ul>
    </div>
  </body>
</html>
